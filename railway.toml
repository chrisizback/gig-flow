[build]
builder = "NIXPACKS"

[deploy]
startCommand = "npm run db:generate && npm run db:push && PUSH_HOST=0.0.0.0 npm run push:dev"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
